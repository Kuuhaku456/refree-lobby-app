import 'package:flutter/material.dart';

class MyLobbyRefree extends StatelessWidget {
  const MyLobbyRefree({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
    );
  }
}